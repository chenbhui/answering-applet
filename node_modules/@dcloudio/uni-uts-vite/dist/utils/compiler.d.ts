import type { parse, bundle, UtsTarget } from '@dcloudio/uts';
export declare function getUtsCompiler(): {
    parse: typeof parse;
    bundle: typeof bundle;
    UtsTarget: typeof UtsTarget;
};
export declare function compile(filename: string): Promise<string | undefined>;
export declare function parsePackage(filepath: string): string;
